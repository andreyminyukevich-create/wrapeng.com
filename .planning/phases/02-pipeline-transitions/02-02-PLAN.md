---
phase: 02-pipeline-transitions
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pipeline-forms.js
  - board.html
autonomous: true
requirements: [FORM-03, FORM-04]

must_haves:
  truths:
    - "The done modal captures checked_by name and remarks, and transitions to status 'done' (not 'waiting')"
    - "The delivery modal shows 5 payment methods: cash, card/transfer, bank transfer with VAT%, deferred, mixed with per-method breakdown"
    - "Delivery modal saves payment_breakdown JSONB to delivery_acts and mirrors total_amount to calculations.final_price"
    - "Mixed payment mode shows individual amount inputs for each sub-method and auto-sums them"
    - "Bank transfer payment mode shows a VAT% input field"
    - "Existing modalDone and modalDeliver HTML are removed from board.html (moved to pipeline-forms.js)"
    - "Existing bindEvents() Done/Deliver handlers are removed from board.html"
  artifacts:
    - path: "pipeline-forms.js"
      provides: "Done and delivery modals"
      contains: "openModalDone"
    - path: "pipeline-forms.js"
      provides: "Delivery modal with 5 payment methods"
      contains: "openModalDeliver"
    - path: "board.html"
      provides: "Clean board without legacy modal HTML"
  key_links:
    - from: "pipeline-forms.js openModalDone"
      to: "window._updateStatus"
      via: "updateStatus call with status 'done'"
      pattern: "_updateStatus.*'done'"
    - from: "pipeline-forms.js saveDeliver"
      to: "delivery_acts table"
      via: "sb.from('delivery_acts').insert"
      pattern: "from\\('delivery_acts'\\)"
    - from: "pipeline-forms.js saveDeliver"
      to: "calculations.final_price"
      via: "updateStatus extra parameter"
      pattern: "final_price.*totalAmount"
---

<objective>
Migrate and upgrade the done modal (FORM-03) and delivery modal (FORM-04) from board.html into pipeline-forms.js, replacing the minimal existing modals with full-featured versions. The done modal adds checked_by/remarks fields and fixes the waiting->done status bug. The delivery modal adds 5 payment methods with dynamic breakdown UI and writes to delivery_acts table.

Purpose: Complete the done and delivery transitions with proper data capture, fix the waiting status bug, and consolidate all modal logic in pipeline-forms.js.
Output: pipeline-forms.js extended with done+deliver modals, board.html cleaned of legacy modal code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipeline-transitions/02-RESEARCH.md

<interfaces>
<!-- Code that MUST be migrated from board.html to pipeline-forms.js -->

From board.html — modalDone HTML (lines 443-471): Will be REMOVED from board.html.
From board.html — modalDeliver HTML (lines 474-499): Will be REMOVED from board.html.
From board.html — openModalDone (lines 1294-1299): Will be REMOVED.
From board.html — extractMaterials (lines 1301-1331): MUST be migrated to pipeline-forms.js.
From board.html — loadDoneMaterials (lines 1333-1387): MUST be migrated to pipeline-forms.js.
From board.html — updateDoneTotals (lines 1389-1402): MUST be migrated to pipeline-forms.js.
From board.html — openModalDeliver (lines 1405-1411): Will be REMOVED.
From board.html — bindEvents Done handlers (lines 1423-1439): Will be REMOVED.
From board.html — bindEvents Deliver handlers (lines 1441-1454): Will be REMOVED.
From board.html — _donePlanItems state variable (line 515): Will be REMOVED from board.html, moved to pipeline-forms.js.

Window globals available to pipeline-forms.js:
```javascript
window._boardCtx = studioCtx;       // { studioId, user, role }
window._updateStatus = updateStatus; // async (calcId, status, extra) => bool
window._loadBoard = loadBoard;       // async () => void
window._sb = sb;                     // QueryBuilder
window._showToast = showToast;       // (type, text) => void
window._closeModal = id => ...;      // close modal by ID
```

From board.html fmt() function (line 572):
```javascript
function fmt(n) {
  return new Intl.NumberFormat('ru-RU', { minimumFractionDigits: 0, maximumFractionDigits: 0 }).format(n || 0);
}
```
Note: fmt() is module-scoped in board.html. pipeline-forms.js must define its own copy or use window._fmt if exposed. Recommendation: define a local `_fmt` in the IIFE.

delivery_acts table columns:
```sql
-- id, calc_id, studio_id, payment_method, payment_breakdown (JSONB), total_amount, vat_percent, notes, delivered_by, created_at
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add done and delivery modals to pipeline-forms.js with materials migration and 5-method payment</name>
  <files>pipeline-forms.js</files>
  <read_first>
    - pipeline-forms.js (current IIFE — add new modals following same pattern)
    - board.html lines 443-471 (modalDone HTML to understand current structure)
    - board.html lines 474-499 (modalDeliver HTML to understand current structure)
    - board.html lines 1293-1411 (openModalDone, extractMaterials, loadDoneMaterials, updateDoneTotals, openModalDeliver — ALL must be migrated)
    - board.html lines 1423-1454 (bindEvents Done/Deliver save handlers — logic to replicate)
    - board.html lines 571-574 (fmt function — need local copy)
  </read_first>
  <action>
Add 2 major modals to pipeline-forms.js IIFE. These are more complex than the outsource/return/cancel modals because they include materials loading logic (done) and dynamic payment UI (deliver).

**Local utility (add near top of IIFE, after 'use strict'):**
```javascript
function _fmt(n) {
  return new Intl.NumberFormat('ru-RU', { minimumFractionDigits: 0, maximumFractionDigits: 0 }).format(n || 0);
}
```

**FORM-03: Done/Check Modal (openModalDone)**

Private state: `let _doneCalcId = null; let _donePlanItems = [];`

HTML template constant `DONE_HTML` — enhanced version of existing modalDone with added check fields:
```html
<div class="modal" id="modalDone">
  <div class="modal-content" style="max-width:540px;max-height:90vh;overflow-y:auto">
    <div class="modal-title">Работа выполнена</div>
    <div class="modal-car" id="doneCarName"></div>

    <div style="font-size:0.69rem;font-weight:700;text-transform:uppercase;letter-spacing:0.07em;color:var(--text-muted);margin-bottom:8px;margin-top:4px">Проверка</div>
    <div class="form-group">
      <label>Кто проверил</label>
      <input type="text" id="doneCheckedBy" placeholder="ФИО проверяющего">
    </div>
    <div class="form-group">
      <label>Замечания / дефекты</label>
      <textarea id="doneRemarks" rows="2" placeholder="Замечания по качеству работы..."></textarea>
    </div>

    <div id="doneMaterialsBlock" style="margin-bottom:16px">
      <div style="font-size:0.69rem;font-weight:700;text-transform:uppercase;letter-spacing:0.07em;color:var(--text-muted);margin-bottom:10px;margin-top:12px">Расход материалов</div>
      <div id="doneMaterialsLoading" style="color:var(--text-muted);font-size:0.82rem;text-align:center;padding:12px 0">Загрузка...</div>
      <div id="doneMaterialsRows" style="display:none">
        <div style="display:grid;grid-template-columns:1fr 110px 110px;gap:6px;margin-bottom:6px;padding:0 2px">
          <div style="font-size:0.67rem;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:var(--text-muted)">Услуга</div>
          <div style="font-size:0.67rem;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:var(--text-muted);text-align:right">План</div>
          <div style="font-size:0.67rem;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:#7c3aed;text-align:right">Факт</div>
        </div>
        <div id="doneMaterialsList" style="display:flex;flex-direction:column;gap:5px"></div>
        <div style="display:grid;grid-template-columns:1fr 110px 110px;gap:6px;margin-top:10px;padding-top:10px;border-top:1px solid var(--border)">
          <div style="font-size:0.8rem;font-weight:700;color:var(--text)">Итого</div>
          <div id="donePlanTotal" style="font-size:0.88rem;font-weight:700;color:var(--text-muted);text-align:right">—</div>
          <div id="doneFactTotal" style="font-size:0.88rem;font-weight:700;color:#7c3aed;text-align:right">—</div>
        </div>
        <div id="doneDelta" style="margin-top:6px;font-size:0.78rem;text-align:right;color:var(--text-muted)"></div>
      </div>
      <div id="doneMaterialsEmpty" style="display:none;color:var(--text-muted);font-size:0.82rem;padding:8px 0">В расчёте нет плановых материалов.</div>
    </div>

    <div class="form-group">
      <label>Примечания</label>
      <textarea id="doneNotes" rows="2" placeholder="Дополнительные заметки..."></textarea>
    </div>

    <div class="modal-footer">
      <button class="btn btn-secondary" id="btnDoneCancel">Отмена</button>
      <button class="btn" style="background:var(--purple);color:#fff" id="btnDoneSave">Завершить</button>
    </div>
  </div>
</div>
```

Migrate these functions from board.html EXACTLY (copy logic, adapt to use `_fmt` instead of `fmt`):
- `extractMaterials(calcData)` — unchanged logic, returns items array
- `loadDoneMaterials(calcId)` — loads calculation_data and fact_materials, builds material rows. Uses `window._sb` instead of `sb`. Uses `_fmt` instead of `fmt`.
- `updateDoneTotals()` — unchanged logic, uses `_fmt` instead of `fmt`.

`injectDone()`: getElementById check, insertAdjacentHTML, bind:
- btnDoneCancel -> close modal
- btnDoneSave -> saveDone()
- overlay click -> close modal

`openModalDone(calcId, carName)`: inject, set _doneCalcId, carName via .textContent, reset checkedBy='', remarks='', notes='', call loadDoneMaterials(calcId), show modal.

`saveDone()`:
1. Button lock pattern.
2. Gather fact_materials from inputs (same as existing bindEvents handler).
3. Gather check data: `const checkedBy = document.getElementById('doneCheckedBy').value.trim(); const remarks = document.getElementById('doneRemarks').value.trim(); const notes = document.getElementById('doneNotes').value.trim();`
4. Build extra object: `const extra = {}; if (Object.keys(factMaterials).length > 0) extra.fact_materials = factMaterials;`
5. Build history comment with check info: `const parts = []; if (checkedBy) parts.push('Проверил: ' + checkedBy); if (remarks) parts.push('Замечания: ' + remarks); if (notes) parts.push(notes); if (parts.length) extra._historyComment = parts.join('; ');`
6. **CRITICAL: Use status 'done' NOT 'waiting'**: `const ok = await window._updateStatus(_doneCalcId, 'done', extra);`
7. On success: close modal, toast 'Работа завершена', loadBoard.

Also handle `waiting` backward compatibility: add to board.html's `normalizeStatus()`: `if (status === 'waiting') return 'done';` — this ensures old `waiting` cards display correctly. (This small edit goes in Task 2.)

**FORM-04: Delivery Modal (openModalDeliver)**

Private state: `let _deliverCalcId = null;`

Constants:
```javascript
const PAYMENT_METHODS = [
  { value: 'cash',     label: 'Наличные',       icon: '' },
  { value: 'card',     label: 'Карта / перевод', icon: '' },
  { value: 'transfer', label: 'Безналичный р/с', icon: '' },
  { value: 'deferred', label: 'Отложенная оплата (долг)', icon: '' },
  { value: 'mixed',    label: 'Смешанная оплата', icon: '' },
];
```

HTML template constant `DELIVER_HTML`:
```html
<div class="modal" id="modalDeliver">
  <div class="modal-content" style="max-width:540px;max-height:90vh;overflow-y:auto">
    <div class="modal-title">Выдать клиенту</div>
    <div class="modal-car" id="deliverCarName"></div>

    <div class="form-group">
      <label>Итоговая сумма</label>
      <input type="number" id="deliverPrice" placeholder="Итого к оплате" min="0">
    </div>

    <div class="form-group">
      <label>Форма оплаты</label>
      <select id="deliverPayment"></select>
    </div>

    <div id="deliverVatBlock" style="display:none">
      <div class="form-group">
        <label>НДС %</label>
        <input type="number" id="deliverVat" placeholder="напр. 20" min="0" max="100" step="0.01">
      </div>
    </div>

    <div id="deliverMixedBlock" style="display:none">
      <div style="font-size:0.69rem;font-weight:700;text-transform:uppercase;letter-spacing:0.07em;color:var(--text-muted);margin-bottom:8px;margin-top:4px">Разбивка по методам</div>
      <div class="form-group">
        <label>Наличные</label>
        <input type="number" id="mixedCash" placeholder="0" min="0" class="mixed-input">
      </div>
      <div class="form-group">
        <label>Карта / перевод</label>
        <input type="number" id="mixedCard" placeholder="0" min="0" class="mixed-input">
      </div>
      <div class="form-group">
        <label>Безналичный р/с</label>
        <input type="number" id="mixedTransfer" placeholder="0" min="0" class="mixed-input">
      </div>
      <div class="form-group">
        <label>Долг</label>
        <input type="number" id="mixedDeferred" placeholder="0" min="0" class="mixed-input">
      </div>
      <div id="mixedTotal" style="font-size:0.82rem;font-weight:700;color:var(--text);text-align:right;margin-top:4px"></div>
    </div>

    <div class="form-group">
      <label>Кто выдал</label>
      <input type="text" id="deliverBy" placeholder="ФИО">
    </div>

    <div class="form-group">
      <label>Комментарий</label>
      <textarea id="deliverNote" rows="2" placeholder="Пожелания, замечания..."></textarea>
    </div>

    <div class="modal-footer">
      <button class="btn btn-secondary" id="btnDeliverCancel">Отмена</button>
      <button class="btn" style="background:var(--success);color:#fff" id="btnDeliverSave">Выдать</button>
    </div>
  </div>
</div>
```

`injectDeliver()`:
1. getElementById check, insertAdjacentHTML.
2. Populate payment method select dynamically from PAYMENT_METHODS.
3. Bind payment method change handler to show/hide VAT and mixed blocks:
```javascript
document.getElementById('deliverPayment').addEventListener('change', function() {
  const val = this.value;
  document.getElementById('deliverVatBlock').style.display = val === 'transfer' ? 'block' : 'none';
  document.getElementById('deliverMixedBlock').style.display = val === 'mixed' ? 'block' : 'none';
});
```
4. Bind mixed-input change handlers to update mixedTotal:
```javascript
document.querySelectorAll('.mixed-input').forEach(inp => {
  inp.addEventListener('input', updateMixedTotal);
});
```
5. Bind cancel/save/overlay.

`updateMixedTotal()`:
```javascript
function updateMixedTotal() {
  const cash = parseFloat(document.getElementById('mixedCash').value) || 0;
  const card = parseFloat(document.getElementById('mixedCard').value) || 0;
  const transfer = parseFloat(document.getElementById('mixedTransfer').value) || 0;
  const deferred = parseFloat(document.getElementById('mixedDeferred').value) || 0;
  const total = cash + card + transfer + deferred;
  document.getElementById('mixedTotal').textContent = 'Итого: ' + _fmt(total);
}
```

`openModalDeliver(calcId, carName, price)`: inject, set _deliverCalcId, carName via .textContent, set deliverPrice.value = price, reset payment to first option, hide VAT and mixed blocks, clear all mixed inputs, clear deliverBy, clear deliverNote, show modal.

`gatherDeliverForm()`:
```javascript
function gatherDeliverForm() {
  const method = document.getElementById('deliverPayment').value;
  const totalAmount = parseFloat(document.getElementById('deliverPrice').value) || 0;
  const vatPercent = method === 'transfer' ? (parseFloat(document.getElementById('deliverVat').value) || null) : null;
  const notes = document.getElementById('deliverNote').value.trim() || null;
  const deliveredBy = document.getElementById('deliverBy').value.trim() || null;

  let paymentBreakdown = {};
  if (method === 'mixed') {
    paymentBreakdown = {
      cash: parseFloat(document.getElementById('mixedCash').value) || 0,
      card: parseFloat(document.getElementById('mixedCard').value) || 0,
      transfer: parseFloat(document.getElementById('mixedTransfer').value) || 0,
      deferred: parseFloat(document.getElementById('mixedDeferred').value) || 0,
    };
  } else {
    paymentBreakdown = { [method]: totalAmount };
  }

  return { payment_method: method, payment_breakdown: paymentBreakdown, total_amount: totalAmount, vat_percent: vatPercent, notes, delivered_by: deliveredBy };
}
```

`saveDeliver()`: Two-step pattern:
1. Button lock.
2. `const payload = gatherDeliverForm();`
3. INSERT delivery_acts: `await window._sb.from('delivery_acts').insert({ calc_id: _deliverCalcId, studio_id: window._boardCtx.studioId, ...payload });`
4. On insert error: toast error, return.
5. **CRITICAL: Mirror total to final_price**: `const ok = await window._updateStatus(_deliverCalcId, 'delivered', { final_price: payload.total_amount, delivery_note: payload.notes });`
6. On success: close modal, toast 'Автомобиль выдан', loadBoard.

**Update window.PipelineForms** to include openModalDone and openModalDeliver:
```javascript
window.PipelineForms = { openModalAccept, openModalOutsource, openModalReturn, openModalCancel, openModalDone, openModalDeliver };
```

Use section dividers: `// -- Модал проверки (done) ----`, `// -- Модал выдачи (deliver) ----`.
  </action>
  <verify>
    <automated>grep -c 'openModalDone\|openModalDeliver' pipeline-forms.js | grep -q '[2-9]' && grep 'window.PipelineForms' pipeline-forms.js | grep -q 'openModalDone' && grep -q 'delivery_acts' pipeline-forms.js && grep -q 'final_price' pipeline-forms.js && grep -q 'PAYMENT_METHODS' pipeline-forms.js && grep -q 'extractMaterials' pipeline-forms.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline-forms.js contains `function openModalDone(calcId, carName)`
    - pipeline-forms.js contains `function openModalDeliver(calcId, carName, price)`
    - pipeline-forms.js contains `function extractMaterials(calcData)` (migrated from board.html)
    - pipeline-forms.js contains `function loadDoneMaterials(calcId)` (migrated)
    - pipeline-forms.js contains `function updateDoneTotals()` (migrated)
    - pipeline-forms.js contains `function saveDone()` with `_updateStatus(_doneCalcId, 'done'` (NOT 'waiting')
    - pipeline-forms.js contains `function saveDeliver()` with `from('delivery_acts').insert(`
    - pipeline-forms.js contains `final_price: payload.total_amount` in updateStatus extra
    - pipeline-forms.js contains `PAYMENT_METHODS` array with 5 entries
    - pipeline-forms.js contains `id="doneCheckedBy"` and `id="doneRemarks"` in DONE_HTML
    - pipeline-forms.js contains `deliverVatBlock` and `deliverMixedBlock` elements for dynamic UI
    - pipeline-forms.js contains `function updateMixedTotal()` for live sum
    - pipeline-forms.js contains `function _fmt(n)` local formatting utility
    - `window.PipelineForms` includes all 6 functions
  </acceptance_criteria>
  <done>pipeline-forms.js has done and delivery modals with full materials migration, check fields, 5-method payment with dynamic mixed/VAT UI, and delivery_acts persistence</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy modals from board.html and rewire done/deliver to PipelineForms</name>
  <files>board.html</files>
  <read_first>
    - board.html lines 442-499 (modalDone + modalDeliver HTML to REMOVE)
    - board.html lines 1293-1411 (openModalDone, extractMaterials, loadDoneMaterials, updateDoneTotals, openModalDeliver — to REMOVE)
    - board.html lines 1417-1454 (bindEvents Done/Deliver handlers — to REMOVE)
    - board.html lines 510-518 (state variables — _donePlanItems to REMOVE)
    - board.html lines 579-588 (normalizeStatus — add waiting->done mapping)
    - board.html lines 1196-1203 (doDone/doDeliver — rewire to PipelineForms)
    - board.html lines 768-793 (onDrop — rewire done/deliver to PipelineForms)
  </read_first>
  <action>
Make these targeted edits to board.html. Order matters — remove from bottom-up to preserve line numbers.

**Edit 1: Remove `_donePlanItems` state variable (around line 515)**
Delete the line: `let _donePlanItems = [];`

**Edit 2: Add waiting->done mapping in normalizeStatus() (around line 580-588)**
Add this line after `if (status === 'Отказ') return 'cancelled';`:
```javascript
if (status === 'waiting') return 'done';
```

**Edit 3: Remove modalDone HTML (lines 442-471)**
Delete the entire `<div class="modal" id="modalDone">...</div>` block.

**Edit 4: Remove modalDeliver HTML (lines 474-499)**
Delete the entire `<div class="modal" id="modalDeliver">...</div>` block.

**Edit 5: Rewire doDone and doDeliver in drawer (around lines 1196-1203)**
Change `doDone` from:
```javascript
const doDone = () => {
    closeDrawer();
    openModalDone(calc.id, calc.car_name || 'Без названия');
  };
```
to:
```javascript
const doDone = () => {
    closeDrawer();
    PipelineForms.openModalDone(calc.id, calc.car_name || 'Без названия');
  };
```

Change `doDeliver` from:
```javascript
const doDeliver = () => {
    closeDrawer();
    openModalDeliver(calc.id, calc.car_name || 'Без названия', calc.final_price || calc.total_price || 0);
  };
```
to:
```javascript
const doDeliver = () => {
    closeDrawer();
    PipelineForms.openModalDeliver(calc.id, calc.car_name || 'Без названия', calc.final_price || calc.total_price || 0);
  };
```

**Edit 6: Rewire onDrop done/delivered (around lines 777-784)**
Change:
```javascript
if (toStatus === 'done') {
    openModalDone(_dragCalcId, carName);
    return;
  }
  if (toStatus === 'delivered') {
    openModalDeliver(_dragCalcId, carName, calc?.total_price || 0);
    return;
  }
```
to:
```javascript
if (toStatus === 'done') {
    PipelineForms.openModalDone(_dragCalcId, carName);
    return;
  }
  if (toStatus === 'delivered') {
    PipelineForms.openModalDeliver(_dragCalcId, carName, calc?.total_price || 0);
    return;
  }
```

**Edit 7: Remove legacy functions (openModalDone, extractMaterials, loadDoneMaterials, updateDoneTotals, openModalDeliver)**
Delete these entire function blocks from board.html (approximately lines 1293-1411):
- `function openModalDone(calcId, carName)` (5 lines)
- `function extractMaterials(calcData)` (~30 lines)
- `async function loadDoneMaterials(calcId)` (~55 lines)
- `function updateDoneTotals()` (~14 lines)
- `function openModalDeliver(calcId, carName, price)` (7 lines)

**Edit 8: Remove Done/Deliver event bindings from bindEvents()**
In the `bindEvents()` function, remove these blocks:
- The `// Модал Done` comment and the 3 handlers (btnDoneCancel click, btnDoneSave click with async handler, modalDone overlay click) — approximately lines 1423-1439
- The `// Модал Deliver` comment and the 3 handlers (btnDeliverCancel click, btnDeliverSave click with async handler, modalDeliver overlay click) — approximately lines 1441-1454

Do NOT remove the drawer, search, filter, or ESC handler bindings.

**Edit 9: Also remove the `waiting` drawer section (around lines 1243-1249)**
The `} else if (status === 'waiting') {` block is dead code since normalizeStatus now maps `waiting` to `done`. Remove it entirely:
```javascript
  } else if (status === 'waiting') {
    el.append(
      btn('Открыть расчёт', goCalc),
      btn('Назначить ответственных', goAssign),
      btn('Выдать клиенту', doDeliver, 'btn-primary'),
    );
  }
```
  </action>
  <verify>
    <automated>grep -c 'function openModalDone\|function extractMaterials\|function loadDoneMaterials\|function updateDoneTotals\|function openModalDeliver' board.html | grep -q '^0$' && grep -q "PipelineForms.openModalDone" board.html && grep -q "PipelineForms.openModalDeliver" board.html && grep -q "waiting.*return.*done" board.html && ! grep -q 'id="modalDone"' board.html && ! grep -q 'id="modalDeliver"' board.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - board.html does NOT contain `<div class="modal" id="modalDone">` (HTML removed)
    - board.html does NOT contain `<div class="modal" id="modalDeliver">` (HTML removed)
    - board.html does NOT contain `function openModalDone` (function removed)
    - board.html does NOT contain `function extractMaterials` (function removed)
    - board.html does NOT contain `function loadDoneMaterials` (function removed)
    - board.html does NOT contain `function updateDoneTotals` (function removed)
    - board.html does NOT contain `function openModalDeliver` (function removed)
    - board.html does NOT contain `let _donePlanItems` (state variable removed)
    - board.html does NOT contain `btnDoneSave` event binding in bindEvents
    - board.html does NOT contain `btnDeliverSave` event binding in bindEvents
    - board.html DOES contain `PipelineForms.openModalDone` in doDone and onDrop
    - board.html DOES contain `PipelineForms.openModalDeliver` in doDeliver and onDrop
    - board.html normalizeStatus contains `if (status === 'waiting') return 'done';`
    - board.html does NOT contain `} else if (status === 'waiting') {` drawer block
    - board.html still contains drawer close, search, filter, and ESC bindings (not accidentally deleted)
  </acceptance_criteria>
  <done>board.html is cleaned of all legacy done/deliver modal code; all references route through PipelineForms; waiting status maps to done for backward compatibility</done>
</task>

</tasks>

<verification>
1. `grep 'function openModalDone\|function openModalDeliver\|function extractMaterials' board.html` returns nothing (functions removed from board.html)
2. `grep 'function openModalDone\|function openModalDeliver\|function extractMaterials' pipeline-forms.js` returns matches (functions migrated)
3. `grep 'PipelineForms.openModal' board.html` shows Done, Deliver, Accept, Outsource, Return, Cancel references
4. `grep 'window.PipelineForms' pipeline-forms.js` shows all 6 modal functions
5. `grep 'id="modalDone"\|id="modalDeliver"' board.html` returns nothing (HTML removed)
6. `grep "'waiting'" board.html` only appears in normalizeStatus mapping and possibly ACTIVE_KEYS/STATUS_COLORS (not in transition logic)
</verification>

<success_criteria>
- Done modal captures check data (checked_by, remarks) and transitions to 'done' not 'waiting'
- Delivery modal has 5 payment methods with dynamic VAT/mixed fields
- Delivery saves to delivery_acts with JSONB payment_breakdown and mirrors to final_price
- All modal HTML and logic lives in pipeline-forms.js (nothing in board.html)
- Existing materials plan/fact comparison UI preserved exactly
- No references to old openModalDone/openModalDeliver functions remain in board.html
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-transitions/02-02-SUMMARY.md`
</output>
